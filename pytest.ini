[pytest]
; The user's Windows temp dir (pytest-of-<user>) is permission-denied for
; scandir in this environment, which breaks the tmp_path fixture. Route
; pytest's basetemp into the repo instead (gitignored) so tmp_path works.
addopts = --basetemp=.pytest-tmp
