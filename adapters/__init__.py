"""Adapters translate a specific agent's result shape into an AgentOutcome.

None of these are imported by ``agent_eval`` itself — they're consumers of
the toolkit, kept in the same repo as worked examples and as the pieces the
benchmarks under ``benchmarks/`` drive.
"""
